node_modules/
.env
.env.*
!.env.example
dist/
.next/
# A second dev server needs its own build dir (see apps/web/next.config.ts)
.next-verify/
*.log
.DS_Store
# Database dumps: every guest's name and phone number, in one file. These
# belong on a disk you control, never in a repository.
backups/
*.dump
